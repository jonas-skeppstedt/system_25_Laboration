# Laboration System25 – TDD med Advent of Code

Laborationsinstruktionerna finns i [LABORATION.md](LABORATION.md).

Repot innehåller:

- `Common` – hjälpklassen `Input` för att läsa indata, med tester i `Common.Tests`.
- `Y2021Day01` – ett färdigt uppsatt projektpar för det första pusslet, med kommenterad
  `Program.cs`, en tom `Sonar.cs` och ett första test som är rött med flit.

Kör `dotnet test`. `Common.Tests` ska vara grönt, `Y2021Day01.Tests` ska vara rött.
Börja där.
